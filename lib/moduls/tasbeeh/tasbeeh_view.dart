import 'package:flutter/material.dart';

class TasbeehView extends StatelessWidget {
  const TasbeehView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: const Text('fsdfds'));
  }
}
